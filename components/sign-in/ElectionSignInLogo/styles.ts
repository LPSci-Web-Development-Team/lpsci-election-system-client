import { BlockOverrides } from 'baseui/block';

export const LOGO: BlockOverrides = {
  Block: {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginTop: '20px',
    },
  },
};
