import { BlockOverrides } from 'baseui/block';

export const HEADING: BlockOverrides = {
  Block: {
    style: {
      fontWeight: 700,
      marginTop: '-10px',
      marginBottom: '20px',
    },
  },
};
